extends layout

block body
  div(style={'width': '100%', 'height': '300px', 'line-height': '300px', 'margin': '50px auto', 'text-align': 'center'})
    | #{status === 404 ? lang.resource_nofound : lang.maintain}